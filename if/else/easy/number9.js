// Enunciado: Crie uma função que recebe um número. Retorne true se o número estiver entre 10 e 20 (inclusive), ou se for maior que 50. 
// Caso contrário, retorne false.
//FUCK OFF COPILOT

function verifyNumber(number) {
    if ((number >= 10 && number <= 20) || number >= 50) {
        return true;
    } return false
}